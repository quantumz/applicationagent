[pytest]
markers =
    ui: Playwright UI tests (require running Flask + Chromium browser)
    live: smoke test against real Anthropic API — requires --live-api flag and ANTHROPIC_API_KEY
    integration: Flask + DB integration tests (no live API) — deselect with -m 'not integration'
