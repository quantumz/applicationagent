#ApplicationAgent

# API key is stored securely in the database — do not add it here.
# Enter your key via the UI prompt on first launch, or the CLI prompt during install.
# Get a key at: https://console.anthropic.com
